name: Synchronize branches backwardly
on:
  push:
    branches:
      - main
      - staging
permissions:
  contents: write

jobs:
  sync-branch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Merge main -> staging
        uses: devmasx/merge-branch@master
        with:
          type: now
          from_branch: main
          target_branch: staging
          github_token: ${{ secrets.ADMIN_PAT }}
          message: "Backward merge action from main branch"
      - name: Merge staging -> develop
        uses: devmasx/merge-branch@master
        with:
          type: now
          from_branch: staging
          target_branch: develop
          github_token: ${{ secrets.ADMIN_PAT }}
          message: "Backward merge action from staging branch"
