// This constant is used to define the domain for the application. The values must match the direct subdirectory strings of the app directory.
export const APP_DOMAIN = {
  PORTFOLIO: "portfolio",
  AUTHENTICATION: "auth",
};

// Constant for private network domains
export const PRIVATE_DOMAIN = ["localhost", "127.0.0.1", "0.0.0.0", "[::1]"];
